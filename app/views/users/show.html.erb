<div class="mt-4">
  <h2>Dashboard</h2>

  <h3>Leagues</h3>
  <table class="table">
    <thead>
      <tr>
        <th scope="col">Name</th>
        <th scope="col">Location</th>
        <th scope="col">League Owner</th>
        <th scope="col">Role</th>
      </tr>
    </thead>
    <tbody>
      <% @leagues.each do |league| %>
        <tr>
          <td><%= league.name %></td>
          <td><%= league.location %></td>
          <td><%= league.owner_name %></td>
          <td><%= current_user.role_in_league(league) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>